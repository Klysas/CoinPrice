<UserControl x:Class="CoinPrice.ContentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CoinPrice"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

	<UserControl.Resources>

		<DataTemplate DataType="{x:Type local:UserCoinData}">
			<Border BorderBrush="Black" BorderThickness="1" Margin="0,0,1,1">
				<DockPanel HorizontalAlignment="Center" VerticalAlignment="Stretch" Height="200">
                    <TextBlock Text="{Binding CoinName}" Margin="10,0" HorizontalAlignment="Center"/>
				</DockPanel>
			</Border>
		</DataTemplate>

	</UserControl.Resources>

	<DockPanel>
		<ItemsControl ItemsSource="{Binding Coins}"/>
	</DockPanel>
</UserControl>
